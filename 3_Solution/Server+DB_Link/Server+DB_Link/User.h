#pragma once
#include <string>
using namespace std;

class User {
public:
    string username;
    string parola;
    string tipCont;
    string grupaStudii;
    /* User(int id, const string& username, const string& email)
         : id(id), username(username), email(email) {}*/
};